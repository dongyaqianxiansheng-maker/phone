<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.callrecorder">

    <!-- Standard, documented permissions. All are runtime-requested; none are
         "signature" or "system" level permissions, and nothing here relies
         on root or hidden APIs. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ConsentActivity"
            android:exported="false" />

        <!-- Foreground service: required so recording is never silent.
             Android forces a persistent, visible notification for the
             entire lifetime of this service. -->
        <!-- foregroundServiceType is omitted: it was introduced in API 29,
             and compileSdkVersion is pinned to 28 to match the Android 9
             target device, so AAPT doesn't recognize the attribute. The
             service is still a proper foreground service via
             startForeground()/startForegroundService() in code, which is
             what actually produces the persistent notification. -->
        <service android:name=".CallRecorderService"
            android:exported="false" />

    </application>
</manifest>
